.container

	%h1.text-center Movies today
	%p#alert.text-danger= alert  

	- @movie_list.each do |movie|
		.figure.px-2
			=link_to movietimetable_sys_path(movie.id) do 
				= image_tag "#{movie.imagelink}" ,class:"img-thumbnail",width:"400px",height:"400px"
			%figcaption{class:"figure-caption"} Movie : #{movie.name}
