%h1 Listing beverages

%table{id:"beveragetable" ,class:"table table-striped table-sm"}
  %thead
    %tr
      %th Name
      %th Price
      %th showbutton
      %th editbutton
      %th destroybutton

  %tbody
    - @beverages.each do |beverage|
      %tr
        %td= beverage.name
        %td= beverage.price
        %td= link_to 'Show', beverage
        %td= link_to 'Edit', edit_beverage_path(beverage)
        %td= link_to 'Destroy', beverage, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Beverage', new_beverage_path

:javascript
  console.log("Hello")
  $(document).ready(function(){
    var btable = $("#beveragetable").DataTable({});
  });
